stage('init') {
      agent {
        docker {
          image 'hashicorp/terraform:1.9.8'
          args '--entrypoint=""'
        }
      }
      steps {
        dir('homelab/cluster-k8s/terraform') {
          sh '''
            terraform init -no-color
          '''
        }
      }
    }